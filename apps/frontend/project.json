{
  "name": "frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/frontend",
  "projectType": "application",
  "tags": [],
  "targets": {
    "dev": {
      "continuous": true,
      "command": "next dev --webpack",
      "options": {
        "cwd": "apps/frontend",
        "port": 3001
      }
    },
    "start": {
      "options": {
        "port": 3001
      }
    },
    "i18n-extract": {
      "executor": "nx:run-commands",
      "options": {
        "command": "formatjs extract 'src/**/*.{ts,tsx,js,jsx}' --ignore='**/*.d.ts' --ignore='**/*.spec.ts' --ignore='**/*.spec.tsx' --ignore='e2e/**' --out-file src/lang/en.json --id-interpolation-pattern '[sha512:contenthash:base64:6]' --format simple --additional-function-names t --pragma formatjs --throws",
        "cwd": "apps/frontend"
      }
    },
    "i18n-verify": {
      "executor": "nx:run-commands",
      "options": {
        "command": "formatjs verify 'src/lang/**/*.json' --source-locale en --missing-keys --extra-keys",
        "cwd": "apps/frontend"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "apps/frontend"
      }
    },
    "graphql-codegen": {
      "executor": "nx:run-commands",
      "options": {
        "command": "graphql-codegen",
        "cwd": "apps/frontend"
      }
    }
  }
}
