info:
  name: devices
  type: http
  seq: 1

http:
  method: GET
  url: "{{backendUrl}}/api/midi/devices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
