info:
  name: all-fixtures
  type: http
  seq: 1

http:
  method: GET
  url: "{{backendUrl}}/api/fixtures/all-fixtures"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
