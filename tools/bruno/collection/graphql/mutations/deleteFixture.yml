info:
  name: deleteFixture
  type: graphql
  seq: 5

graphql:
  method: POST
  url: "{{backendUrl}}/graphql"
  auth: inherit
  body:
    query: |
      mutation($publicId: UUID!) {
        deleteFixture(publicId: $publicId) {
          deleted
          publicId
        }
      }
    variables: |
      {
        "publicId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
